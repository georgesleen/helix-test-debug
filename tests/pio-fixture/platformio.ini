; Fixture project for the cog's PlatformIO support. `platform = native`
; builds and runs the Unity tests with the host toolchain, so the check needs
; no microcontroller attached.

[env:native]
platform = native
test_framework = unity
