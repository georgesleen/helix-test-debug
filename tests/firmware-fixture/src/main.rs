// Fixture firmware for tests/integration.sh. Its lines are stopped at by the
// check, so their numbering is part of it.

fn main() {
    let mut ticks = 0;
    ticks += 1;
    println!("ticks {ticks}");
}
