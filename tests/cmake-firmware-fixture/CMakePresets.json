{
  "version": 3,
  "configurePresets": [
    {
      "name": "firmware",
      "description": "binaryDir is deliberately nowhere a name list would look, and uses both macros the cog has to expand. The generator is multi-config so the same target appears once per configuration.",
      "generator": "Ninja Multi-Config",
      "binaryDir": "${sourceDir}/out/build/${presetName}",
      "toolchainFile": "${sourceDir}/toolchain-arm.cmake",
      "cacheVariables": {
        "CMAKE_CONFIGURATION_TYPES": "Debug;Release"
      }
    }
  ]
}
