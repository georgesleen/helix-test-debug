diff --git a/helix-dap/src/client.rs b/helix-dap/src/client.rs
index eb45e14..d07b958 100644
--- a/helix-dap/src/client.rs
+++ b/helix-dap/src/client.rs
@@ -47,6 +47,10 @@ pub struct Client {
     pub quirks: DebuggerQuirks,
     /// The config which was used to start this debugger.
     pub config: Option<DebugAdapterConfig>,
+    /// Accumulated session output, excluding telemetry. Reset per session.
+    output: String,
+    /// Whether this session exited with a nonzero code.
+    failed: bool,
 }
 
 impl Client {
@@ -95,6 +99,8 @@ pub fn streams(
             active_frame: None,
             quirks: DebuggerQuirks::default(),
             config: None,
+            output: String::new(),
+            failed: false,
         };
 
         tokio::spawn(Self::recv(id, server_rx, client_tx));
@@ -162,6 +168,31 @@ pub fn starting_request_args(&self) -> Option<&Value> {
         self.starting_request_args.as_ref()
     }
 
+    /// Append output text; chunks are concatenated as-is.
+    pub fn push_output(&mut self, text: &str) {
+        self.output.push_str(text);
+    }
+
+    /// Session output captured so far.
+    pub fn output(&self) -> &str {
+        &self.output
+    }
+
+    /// Take and reset the accumulated session output.
+    pub fn take_output(&mut self) -> String {
+        std::mem::take(&mut self.output)
+    }
+
+    /// Mark this session as having exited with a nonzero code.
+    pub fn mark_failed(&mut self) {
+        self.failed = true;
+    }
+
+    /// Take and reset the failed flag.
+    pub fn take_failed(&mut self) -> bool {
+        std::mem::take(&mut self.failed)
+    }
+
     pub async fn tcp_process(
         cmd: &str,
         args: Vec<&str>,
diff --git a/helix-term/src/commands/engine/steel/misc.scm b/helix-term/src/commands/engine/steel/misc.scm
index 1dca13d..7ff74ad 100644
--- a/helix-term/src/commands/engine/steel/misc.scm
+++ b/helix-term/src/commands/engine/steel/misc.scm
@@ -140,6 +140,33 @@ (provide set-error!)
 ;;Sets the content of the status line, with the error severity
 (define set-error! helix.set-error!)
 
+(provide set-output!)
+;;@doc
+;;Stores `text` (raw text, may contain ANSI SGR colour/style escapes) as the
+;;captured output buffer. Does not display anything by itself; call
+;;`show-output!` afterward to populate and focus the native output scratch
+;;buffer.
+(define set-output! helix.set-output!)
+
+(provide show-output!)
+;;@doc
+;;Parses the captured output buffer's ANSI escapes into plain, searchable
+;;text plus rendered colours/styles, and (re)populates the native output
+;;scratch buffer with it, focusing that buffer. Returns `#f` without doing
+;;anything if there is no captured output (e.g. `set-output!` was never
+;;called, or was called with an empty string).
+(define show-output! helix.show-output!)
+
+(provide output->text)
+;;@doc
+;;Strips ANSI escape sequences (SGR colour/style codes, other CSI
+;;sequences, OSC sequences) from `text`, returning plain text only. Uses the
+;;same parser as the native output buffer, so callers that need plain text
+;;(e.g. to feed a subprocess's captured output through a text-only parser,
+;;or onto the status line) don't need a second ANSI-stripping
+;;implementation.
+(define output->text helix.output->text)
+
 (provide send-lsp-command)
 ;;@doc
 ;; Send an lsp command. The `lsp-name` must correspond to an active lsp.
diff --git a/helix-term/src/commands/engine/steel/mod.rs b/helix-term/src/commands/engine/steel/mod.rs
index 2fd1526..e238c97 100644
--- a/helix-term/src/commands/engine/steel/mod.rs
+++ b/helix-term/src/commands/engine/steel/mod.rs
@@ -4050,6 +4050,9 @@ fn load_misc_api(engine: &mut Engine, generate_sources: bool) {
         .register_fn_with_ctx(CTX, "set-status!", set_status)
         .register_fn_with_ctx(CTX, "set-warning!", set_warning)
         .register_fn_with_ctx(CTX, "set-error!", set_error)
+        .register_fn_with_ctx(CTX, "set-output!", set_output)
+        .register_fn_with_ctx(CTX, "show-output!", show_output)
+        .register_fn("output->text", output_to_text)
         .register_fn_with_ctx(CTX, "send-lsp-command", send_arbitrary_lsp_command)
         .register_fn_with_ctx(
             CTX,
@@ -4981,6 +4984,34 @@ fn set_error(cx: &mut Context, value: SteelVal) {
     }
 }
 
+/// (set-output! text) -> void
+/// Stores `text` (raw, possibly ANSI-decorated) as the captured output
+/// buffer. Does not display anything by itself; pair with `show-output!`.
+fn set_output(cx: &mut Context, value: SteelVal) {
+    match value {
+        SteelVal::StringV(s) => cx.editor.set_output(s.as_ref().to_owned()),
+        _ => cx.editor.set_output(value.to_string()),
+    }
+}
+
+/// (show-output!) -> bool
+/// Parses the captured output buffer's ANSI escapes and (re)populates the
+/// native, coloured, searchable output scratch buffer, focusing it.
+/// Returns `#f` without doing anything if there is no captured output.
+fn show_output(cx: &mut Context) -> bool {
+    cx.editor.show_output()
+}
+
+/// (output->text text) -> string
+/// Strips ANSI escape sequences from `text` using the same tokenizer as the
+/// native output buffer, returning plain text only. Intended for feeding
+/// text-only consumers (e.g. forcing cargo/libtest colour, then stripping
+/// it back out for their own plain-text output parsers or the status line)
+/// without a second, divergent ANSI parser implementation.
+fn output_to_text(text: SteelString) -> String {
+    helix_view::ansi::ansi_to_plain_text(text.as_str())
+}
+
 fn trigger_callback(cx: &mut Context, key: KeyEvent) {
     if let Some(callback) = cx.on_next_key_callback.take() {
         (callback.0)(cx, key);
diff --git a/helix-term/src/ui/document.rs b/helix-term/src/ui/document.rs
index c9db744..7dc2889 100644
--- a/helix-term/src/ui/document.rs
+++ b/helix-term/src/ui/document.rs
@@ -55,6 +55,7 @@ pub fn render_document(
         doc_annotations,
         syntax_highlighter,
         overlay_highlights,
+        doc.output_styles(),
         theme,
         decorations,
     )
@@ -69,6 +70,7 @@ pub fn render_text(
     text_annotations: &TextAnnotations,
     syntax_highlighter: Option<Highlighter<'_>>,
     overlay_highlights: Vec<syntax::OverlayHighlights>,
+    output_styles: &[(std::ops::Range<usize>, Style)],
     theme: &Theme,
     mut decorations: DecorationManager,
 ) {
@@ -81,6 +83,7 @@ pub fn render_text(
     let mut syntax_highlighter =
         SyntaxHighlighter::new(syntax_highlighter, text, theme, renderer.text_style);
     let mut overlay_highlighter = OverlayHighlighter::new(overlay_highlights, theme);
+    let mut output_highlighter = OutputStyleHighlighter::new(output_styles);
 
     let mut last_line_pos = LinePos {
         first_visual_line: false,
@@ -139,6 +142,9 @@ pub fn render_text(
         while grapheme.char_idx >= overlay_highlighter.pos {
             overlay_highlighter.advance();
         }
+        while grapheme.char_idx >= output_highlighter.pos {
+            output_highlighter.advance();
+        }
 
         let grapheme_style = if let GraphemeSource::VirtualText { highlight } = grapheme.source {
             let mut style = renderer.text_style;
@@ -148,11 +154,13 @@ pub fn render_text(
             GraphemeStyle {
                 syntax_style: style,
                 overlay_style: Style::default(),
+                output_style: Style::default(),
             }
         } else {
             GraphemeStyle {
                 syntax_style: syntax_highlighter.style,
                 overlay_style: overlay_highlighter.style,
+                output_style: output_highlighter.style,
             }
         };
         decorations.decorate_grapheme(renderer, &grapheme);
@@ -196,6 +204,10 @@ pub struct TextRenderer<'a> {
 pub struct GraphemeStyle {
     syntax_style: Style,
     overlay_style: Style,
+    /// ANSI-derived style for native output scratch buffers. Always wins
+    /// over syntax/overlay highlighting since it represents the literal
+    /// colours a subprocess/DAP session produced.
+    output_style: Style,
 }
 
 impl<'a> TextRenderer<'a> {
@@ -334,6 +346,7 @@ pub fn draw_grapheme(
             style = style.patch(self.whitespace_style);
         }
         style = style.patch(grapheme_style.overlay_style);
+        style = style.patch(grapheme_style.output_style);
 
         let width = grapheme.width();
         let mut is_tab = false;
@@ -574,3 +587,52 @@ fn advance(&mut self) {
         self.update_pos();
     }
 }
+
+/// Iterates directly over pre-resolved `Style` ranges (as parsed from ANSI
+/// SGR sequences by `helix_view::ansi::parse_ansi`), bypassing the
+/// theme/`Highlight`-index indirection used by `SyntaxHighlighter` and
+/// `OverlayHighlighter`: the styles are already concrete `fg`/`bg`/modifier
+/// values, not theme scopes. Follows the same `pos`/`advance` polling
+/// convention as its siblings so it composes with the same render loop.
+struct OutputStyleHighlighter<'d> {
+    ranges: &'d [(std::ops::Range<usize>, Style)],
+    idx: usize,
+    pos: usize,
+    style: Style,
+}
+
+impl<'d> OutputStyleHighlighter<'d> {
+    fn new(ranges: &'d [(std::ops::Range<usize>, Style)]) -> Self {
+        let mut highlighter = Self {
+            ranges,
+            idx: 0,
+            pos: 0,
+            style: Style::default(),
+        };
+        highlighter.update_pos();
+        highlighter
+    }
+
+    fn update_pos(&mut self) {
+        self.pos = match self.ranges.get(self.idx) {
+            // Not currently inside a styled range: the next event is that
+            // range's start.
+            Some((range, _)) if self.style == Style::default() => range.start,
+            // Currently inside a styled range: the next event is where it
+            // ends.
+            Some((range, _)) => range.end,
+            None => usize::MAX,
+        };
+    }
+
+    fn advance(&mut self) {
+        if self.style != Style::default() {
+            // Leaving the range at `idx`.
+            self.idx += 1;
+            self.style = Style::default();
+        } else if let Some((_, style)) = self.ranges.get(self.idx) {
+            self.style = *style;
+        }
+        self.update_pos();
+    }
+}
diff --git a/helix-view/src/ansi.rs b/helix-view/src/ansi.rs
new file mode 100644
index 0000000..c430bc7
--- /dev/null
+++ b/helix-view/src/ansi.rs
@@ -0,0 +1,302 @@
+//! Minimal ANSI/SGR parser used by the native scratch output buffer.
+//!
+//! Converts raw ANSI-decorated text (as produced by subprocesses run with
+//! `:run-here`/`:debug-here` and captured cargo/libtest/DAP output) into
+//! plain, searchable text plus a list of styled character ranges suitable
+//! for rendering as an overlay on top of a [`Document`](crate::Document).
+//!
+//! Only SGR (`ESC [ ... m`) sequences are interpreted for styling. All other
+//! control sequences (other CSI sequences, OSC sequences, single-character
+//! escapes) are stripped from the output text without affecting styling, so
+//! that terminal escapes never leak into the buffer or its search index.
+//! Malformed/unterminated sequences are dropped defensively; parsing never
+//! panics on arbitrary input.
+
+use std::ops::Range;
+
+use crate::graphics::{Color, Modifier, Style};
+
+const ESC: char = '\u{1b}';
+const BEL: char = '\u{7}';
+
+/// A styled span over the *parsed* (escape-free) output text, expressed as a
+/// half-open char range.
+pub type StyledSpan = (Range<usize>, Style);
+
+/// Parses `input`, returning the plain text with all ANSI escape sequences
+/// removed, along with the list of styled spans (in char offsets into the
+/// returned text) derived from SGR sequences. Spans are emitted in order and
+/// are non-overlapping.
+pub fn parse_ansi(input: &str) -> (String, Vec<StyledSpan>) {
+    let mut text = String::with_capacity(input.len());
+    let mut spans = Vec::new();
+
+    let mut style = Style::default();
+    let mut span_start = 0usize;
+    let mut char_idx = 0usize;
+
+    let mut chars = input.chars().peekable();
+    while let Some(c) = chars.next() {
+        if c != ESC {
+            text.push(c);
+            char_idx += 1;
+            continue;
+        }
+
+        match chars.peek() {
+            Some('[') => {
+                chars.next(); // consume '['
+                let params = consume_csi_params(&mut chars);
+                // Only SGR (final byte 'm') sequences carry styling; other
+                // CSI sequences (cursor movement, erase, etc.) are simply
+                // dropped from the text.
+                if let Some(final_byte) = params.final_byte {
+                    if final_byte == 'm' {
+                        let new_style = apply_sgr(style, &params.args);
+                        if new_style != style {
+                            if char_idx > span_start && !style_is_default(style) {
+                                spans.push((span_start..char_idx, style));
+                            }
+                            style = new_style;
+                            span_start = char_idx;
+                        }
+                    }
+                }
+                // Malformed/unterminated CSI: params.final_byte is None,
+                // nothing further to do; already consumed to end of input.
+            }
+            Some(']') => {
+                chars.next(); // consume ']'
+                consume_osc(&mut chars);
+            }
+            Some(_) => {
+                // Single-character escape (e.g. ESC(B charset select, ESC=,
+                // ESC>). Consume the one following character, if any, and
+                // drop both from the output.
+                chars.next();
+            }
+            None => {
+                // Trailing bare ESC with nothing after it: drop it.
+            }
+        }
+    }
+
+    if char_idx > span_start && !style_is_default(style) {
+        spans.push((span_start..char_idx, style));
+    }
+
+    (text, spans)
+}
+
+/// Strips all ANSI escape sequences from `input`, returning plain text only.
+/// Uses the same tokenizer as [`parse_ansi`] but discards styling
+/// information; intended for feeding text-only consumers (status lines,
+/// cargo/libtest's own plain-text parsers) without requiring a second,
+/// separate parser implementation.
+pub fn ansi_to_plain_text(input: &str) -> String {
+    parse_ansi(input).0
+}
+
+fn style_is_default(style: Style) -> bool {
+    style == Style::default()
+}
+
+struct CsiParams {
+    args: Vec<i64>,
+    final_byte: Option<char>,
+}
+
+/// Consumes CSI parameter/intermediate bytes up to and including the final
+/// byte (0x40..=0x7E), returning the parsed numeric arguments (defaulting
+/// missing/empty fields to no explicit value, represented here as -1 to
+/// distinguish "no value" e.g. bare `\x1b[m` == reset). Returns
+/// `final_byte: None` if the input ends before a final byte is found
+/// (malformed/unterminated sequence); the iterator will have been drained to
+/// the end in that case, which is safe (just stops interpreting further
+/// escapes).
+fn consume_csi_params(chars: &mut std::iter::Peekable<std::str::Chars<'_>>) -> CsiParams {
+    let mut raw = String::new();
+    let mut final_byte = None;
+    for c in chars.by_ref() {
+        if ('\u{40}'..='\u{7e}').contains(&c) {
+            final_byte = Some(c);
+            break;
+        }
+        raw.push(c);
+    }
+
+    let args = raw
+        .split(';')
+        .map(|field| field.parse::<i64>().unwrap_or(-1))
+        .collect();
+
+    CsiParams { args, final_byte }
+}
+
+/// Consumes an OSC (`ESC ] ...`) sequence up to its terminator: either BEL
+/// (`\x07`) or the two-character ST (`ESC \`). Defensive against
+/// unterminated sequences: simply drains to end of input.
+fn consume_osc(chars: &mut std::iter::Peekable<std::str::Chars<'_>>) {
+    while let Some(c) = chars.next() {
+        if c == BEL {
+            return;
+        }
+        if c == ESC {
+            if chars.peek() == Some(&'\\') {
+                chars.next();
+            }
+            return;
+        }
+    }
+}
+
+fn apply_sgr(mut style: Style, args: &[i64]) -> Style {
+    let mut i = 0;
+    // A bare `\x1b[m` parses to a single empty field -> [-1], which is a
+    // reset, matching the "no args" ECMA-48 convention.
+    if args.is_empty() {
+        return Style::default();
+    }
+    while i < args.len() {
+        let code = args[i];
+        match code {
+            -1 | 0 => style = Style::default(),
+            1 => style.add_modifier |= Modifier::BOLD,
+            2 => style.add_modifier |= Modifier::DIM,
+            3 => style.add_modifier |= Modifier::ITALIC,
+            4 => style.underline_style = Some(crate::graphics::UnderlineStyle::Line),
+            5 | 6 => style.add_modifier |= Modifier::SLOW_BLINK,
+            7 => style.add_modifier |= Modifier::REVERSED,
+            8 => style.add_modifier |= Modifier::HIDDEN,
+            9 => style.add_modifier |= Modifier::CROSSED_OUT,
+            21 => style.underline_style = Some(crate::graphics::UnderlineStyle::DoubleLine),
+            22 => {
+                style.add_modifier.remove(Modifier::BOLD | Modifier::DIM);
+            }
+            23 => {
+                style.add_modifier.remove(Modifier::ITALIC);
+            }
+            24 => style.underline_style = None,
+            25 => {
+                style
+                    .add_modifier
+                    .remove(Modifier::SLOW_BLINK | Modifier::RAPID_BLINK);
+            }
+            27 => {
+                style.add_modifier.remove(Modifier::REVERSED);
+            }
+            28 => {
+                style.add_modifier.remove(Modifier::HIDDEN);
+            }
+            29 => {
+                style.add_modifier.remove(Modifier::CROSSED_OUT);
+            }
+            30..=37 => style.fg = Some(ansi_16_color((code - 30) as u8)),
+            38 => {
+                if let Some((color, consumed)) = extended_color(&args[i + 1..]) {
+                    style.fg = Some(color);
+                    i += consumed;
+                } else {
+                    // Malformed extended color sequence: stop interpreting
+                    // remaining args defensively rather than misparsing.
+                    break;
+                }
+            }
+            39 => style.fg = None,
+            40..=47 => style.bg = Some(ansi_16_color((code - 40) as u8)),
+            48 => {
+                if let Some((color, consumed)) = extended_color(&args[i + 1..]) {
+                    style.bg = Some(color);
+                    i += consumed;
+                } else {
+                    break;
+                }
+            }
+            49 => style.bg = None,
+            90..=97 => style.fg = Some(ansi_16_color((code - 90) as u8 + 8)),
+            100..=107 => style.bg = Some(ansi_16_color((code - 100) as u8 + 8)),
+            _ => {}
+        }
+        i += 1;
+    }
+    style
+}
+
+fn ansi_16_color(index: u8) -> Color {
+    match index {
+        0 => Color::Black,
+        1 => Color::Red,
+        2 => Color::Green,
+        3 => Color::Yellow,
+        4 => Color::Blue,
+        5 => Color::Magenta,
+        6 => Color::Cyan,
+        7 => Color::Gray,
+        8 => Color::Gray,
+        9 => Color::LightRed,
+        10 => Color::LightGreen,
+        11 => Color::LightYellow,
+        12 => Color::LightBlue,
+        13 => Color::LightMagenta,
+        14 => Color::LightCyan,
+        _ => Color::White,
+    }
+}
+
+/// Parses the `5;N` (indexed) or `2;R;G;B` (truecolor) tail of a `38`/`48`
+/// SGR extended color sequence. Returns the resolved color and the number of
+/// trailing args consumed (not counting the `38`/`48` selector itself).
+/// Returns `None` on malformed/incomplete input, guarding against panics on
+/// truncated escape sequences.
+fn extended_color(rest: &[i64]) -> Option<(Color, usize)> {
+    match rest.first()? {
+        5 => {
+            let idx = *rest.get(1)?;
+            if !(0..=255).contains(&idx) {
+                return None;
+            }
+            Some((Color::Indexed(idx as u8), 2))
+        }
+        2 => {
+            let r = *rest.get(1)?;
+            let g = *rest.get(2)?;
+            let b = *rest.get(3)?;
+            if ![r, g, b].iter().all(|v| (0..=255).contains(v)) {
+                return None;
+            }
+            Some((Color::Rgb(r as u8, g as u8, b as u8), 4))
+        }
+        _ => None,
+    }
+}
+
+#[cfg(test)]
+mod tests {
+    use super::*;
+
+    #[test]
+    fn truecolor_spans_use_plain_text_character_offsets() {
+        let (text, spans) = parse_ansi("plain \x1b[38;2;1;2;3m彩色\x1b[0m end");
+
+        assert_eq!(text, "plain 彩色 end");
+        assert_eq!(spans.len(), 1);
+        assert_eq!(spans[0].0, 6..8);
+        assert_eq!(spans[0].1.fg, Some(Color::Rgb(1, 2, 3)));
+    }
+
+    #[test]
+    fn terminal_controls_never_enter_searchable_text() {
+        let text =
+            ansi_to_plain_text("before\x1b]8;;https://example.com\x07link\x1b]8;;\x07\x1b[2Kafter");
+
+        assert_eq!(text, "beforelinkafter");
+    }
+
+    #[test]
+    fn incomplete_extended_colour_keeps_following_text_unstyled() {
+        let (text, spans) = parse_ansi("\x1b[38;2;1mvisible");
+
+        assert_eq!(text, "visible");
+        assert!(spans.is_empty());
+    }
+}
diff --git a/helix-view/src/document.rs b/helix-view/src/document.rs
index 17d4002..1b98c04 100644
--- a/helix-view/src/document.rs
+++ b/helix-view/src/document.rs
@@ -216,6 +216,14 @@ pub struct Document {
     pub name: Option<String>,
     pub readonly: bool,
 
+    /// Styled ranges (char offsets into `text`) produced by parsing ANSI
+    /// SGR sequences out of native scratch output (see `crate::ansi`).
+    /// Cleared on every text-changing `apply`, so stale ranges can never
+    /// miscolour content the styling wasn't computed for; repopulated
+    /// atomically by `Editor::show_output` immediately after it rewrites
+    /// the buffer text.
+    pub(crate) output_styles: Vec<(std::ops::Range<usize>, crate::graphics::Style)>,
+
     pub previous_diagnostic_ids: HashMap<LanguageServerId, String>,
 
     /// Annotations for LSP document color swatches
@@ -770,6 +778,7 @@ pub fn from(
             focused_at: std::time::Instant::now(),
             name: None,
             readonly: false,
+            output_styles: Vec::new(),
             jump_labels: HashMap::new(),
             document_highlights: HashMap::new(),
             code_action_hints: HashSet::new(),
@@ -1490,6 +1499,11 @@ fn apply_impl(
 
         self.modified_since_accessed = true;
         self.version += 1;
+        // Any real edit invalidates previously computed ANSI style ranges;
+        // `Editor::show_output`/`set_output` repopulate them immediately
+        // after rewriting the buffer, so this only ever discards ranges
+        // that no longer correspond to the (now different) text.
+        self.output_styles.clear();
 
         for selection in self.selections.values_mut() {
             *selection = selection
@@ -2096,6 +2110,23 @@ pub fn text(&self) -> &Rope {
         &self.text
     }
 
+    /// The ANSI-derived styled ranges (char offsets) for the native output
+    /// scratch buffer, if any were parsed for the current text.
+    #[inline]
+    pub fn output_styles(&self) -> &[(std::ops::Range<usize>, crate::graphics::Style)] {
+        &self.output_styles
+    }
+
+    /// Overwrites the ANSI-derived styled ranges. Callers MUST ensure the
+    /// ranges are valid char offsets into the current `text` and are
+    /// sorted, non-overlapping; this is upheld by `crate::ansi::parse_ansi`.
+    pub(crate) fn set_output_styles(
+        &mut self,
+        styles: Vec<(std::ops::Range<usize>, crate::graphics::Style)>,
+    ) {
+        self.output_styles = styles;
+    }
+
     #[inline]
     pub fn selection(&self, view_id: ViewId) -> &Selection {
         &self.selections[&view_id]
diff --git a/helix-view/src/editor.rs b/helix-view/src/editor.rs
index 3eb14e3..e579e40 100644
--- a/helix-view/src/editor.rs
+++ b/helix-view/src/editor.rs
@@ -1332,6 +1332,16 @@ pub struct Editor {
     pub status_msg: Option<(Cow<'static, str>, Severity)>,
     pub autoinfo: Option<Info>,
 
+    /// Raw (ANSI-decorated) text captured by `:run-here`/`:debug-here`
+    /// subprocess and DAP session output, accumulated via `append_output`
+    /// and replaced wholesale via `set_output`. Consumed by `show_output`,
+    /// which parses it and (re)populates the native output scratch buffer.
+    output_buffer: String,
+    /// The scratch document backing the native output buffer, if one has
+    /// been created and is still open. Reused across calls to avoid
+    /// spawning a fresh scratch buffer every time output is shown.
+    output_doc: Option<DocumentId>,
+
     pub config: Arc<dyn DynAccess<Config>>,
     pub auto_pairs: Option<AutoPairs>,
 
@@ -1485,6 +1495,8 @@ pub fn new(
             ))),
             status_msg: None,
             autoinfo: None,
+            output_buffer: String::new(),
+            output_doc: None,
             idle_timer: Box::pin(sleep(conf.idle_timeout)),
             redraw_timer: Box::pin(sleep(Duration::MAX)),
             last_motion: None,
@@ -1603,6 +1615,74 @@ pub fn is_err(&self) -> bool {
             .unwrap_or(false)
     }
 
+    /// Replaces the raw (ANSI-decorated) captured output text wholesale.
+    /// Does not affect the on-screen scratch buffer until `show_output` is
+    /// called; callers that always want it displayed should call
+    /// `show_output` afterward.
+    pub fn set_output<T: Into<String>>(&mut self, text: T) {
+        self.output_buffer = text.into();
+    }
+
+    /// Appends to the raw captured output text, e.g. for streaming
+    /// subprocess/DAP output as it arrives.
+    pub fn append_output(&mut self, text: &str) {
+        self.output_buffer.push_str(text);
+    }
+
+    /// Returns the currently captured raw output text without consuming or
+    /// displaying it.
+    pub fn output(&self) -> &str {
+        &self.output_buffer
+    }
+
+    /// Parses the captured output text and (re)populates the native output
+    /// scratch buffer with the plain text plus ANSI-derived styling,
+    /// creating the scratch document on first use and reusing it on
+    /// subsequent calls. Returns `false` (and leaves any existing scratch
+    /// buffer untouched) if there is no captured output to show.
+    pub fn show_output(&mut self) -> bool {
+        if self.output_buffer.is_empty() {
+            return false;
+        }
+
+        let (plain_text, styles) = crate::ansi::parse_ansi(&self.output_buffer);
+
+        let doc_id = match self.output_doc.filter(|id| self.documents.contains_key(id)) {
+            Some(id) => id,
+            None => {
+                let mut doc = Document::default(self.config.clone(), self.syn_loader.clone());
+                doc.readonly = true;
+                doc.name = Some("[output]".to_string());
+                let id = self.new_document(doc);
+                self.output_doc = Some(id);
+                id
+            }
+        };
+
+        self.switch(doc_id, Action::Replace);
+        let view_id = self.tree.focus;
+        let doc = self
+            .documents
+            .get_mut(&doc_id)
+            .expect("output_doc was just created or verified present");
+        // Native scratch buffers are otherwise readonly; bypass that for
+        // our own controlled rewrite of the contents.
+        let was_readonly = doc.readonly;
+        doc.readonly = false;
+        let end = doc.text().len_chars();
+        let transaction = helix_core::Transaction::change(
+            doc.text(),
+            std::iter::once((0, end, Some(plain_text.into()))),
+        );
+        doc.apply(&transaction, view_id);
+        doc.set_output_styles(styles);
+        doc.set_selection(view_id, Selection::point(0));
+        doc.readonly = was_readonly;
+        doc.reset_modified();
+
+        true
+    }
+
     pub fn unset_theme_preview(&mut self) -> anyhow::Result<()> {
         if let Some(last_theme) = self.last_theme.take() {
             self.set_theme(last_theme)?;
diff --git a/helix-view/src/handlers/dap.rs b/helix-view/src/handlers/dap.rs
index a8b3d88..a3f1c69 100644
--- a/helix-view/src/handlers/dap.rs
+++ b/helix-view/src/handlers/dap.rs
@@ -319,17 +319,30 @@ pub async fn handle_debugger_message(
                     Event::Output(events::OutputBody {
                         category, output, ..
                     }) => {
+                        if category.as_deref() == Some("telemetry") {
+                            return false;
+                        }
+
+                        log::info!("{}", output);
+
+                        let is_active =
+                            self.debug_adapters.get_active_client().map(|c| c.id()) == Some(id);
+
+                        match self.debug_adapters.get_client_mut(id) {
+                            Some(debugger) => debugger.push_output(&output),
+                            None => return false,
+                        }
+
+                        // Only mirror into the live scratch buffer for the
+                        // active client, so concurrent sessions don't interleave.
+                        if is_active {
+                            self.append_output(&output);
+                        }
+
                         let prefix = match category {
-                            Some(category) => {
-                                if &category == "telemetry" {
-                                    return false;
-                                }
-                                format!("Debug ({}):", category)
-                            }
+                            Some(category) => format!("Debug ({}):", category),
                             None => "Debug:".to_owned(),
                         };
-
-                        log::info!("{}", output);
                         self.set_status(format!("{} {}", prefix, output));
                     }
                     Event::ProgressStart(body) => {
@@ -424,6 +437,15 @@ pub async fn handle_debugger_message(
                             return false;
                         }
 
+                        // Flush output; a trailing chunk between Exited and
+                        // Terminated is included since this runs last.
+                        let failed = debugger.take_failed();
+                        let output_text = debugger.take_output();
+                        self.set_output(output_text);
+                        if failed {
+                            self.show_output();
+                        }
+
                         match restart_arg {
                             Some(Value::Bool(false)) | None => {
                                 self.debug_adapters.remove_client(id);
@@ -442,6 +464,10 @@ pub async fn handle_debugger_message(
                             }
                             Some(val) => {
                                 log::info!("Attempting to restart debug session.");
+                                let debugger = match self.debug_adapters.get_client_mut(id) {
+                                    Some(debugger) => debugger,
+                                    None => return false,
+                                };
                                 let connection_type = match debugger.connection_type() {
                                     Some(connection_type) => connection_type,
                                     None => {
@@ -468,6 +494,18 @@ pub async fn handle_debugger_message(
                     }
                     Event::Exited(resp) => {
                         let exit_code = resp.exit_code;
+
+                        // Snapshot now for visibility; Terminated does the
+                        // final flush and owns the auto-show decision so a
+                        // trailing Output event isn't left stale/unopened.
+                        if let Some(debugger) = self.debug_adapters.get_client_mut(id) {
+                            if exit_code != 0 {
+                                debugger.mark_failed();
+                            }
+                            let output_text = debugger.output().to_string();
+                            self.set_output(output_text);
+                        }
+
                         if exit_code != 0 {
                             self.set_error(format!(
                                 "Debuggee failed to exit successfully (exit code: {exit_code})."
diff --git a/helix-view/src/lib.rs b/helix-view/src/lib.rs
index 38ca8ae..328aaff 100644
--- a/helix-view/src/lib.rs
+++ b/helix-view/src/lib.rs
@@ -1,6 +1,8 @@
 #[macro_use]
 pub mod macros;
 
+pub mod ansi;
+
 pub mod action;
 pub mod annotations;
 pub mod clipboard;
